# TODO

## Testing

- [ ] Add tests for `execute_triage` and file operations
  - `execute_triage` with edge cases (empty arrays, missing files)
  - `move_to_trash` handling
  - Zustand store classification logic
